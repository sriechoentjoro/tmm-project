# Authorization System - Visual Flow Diagrams

## 📊 Authorization Flow Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        User Request                              │
│                     (e.g., /candidates/edit/5)                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    beforeFilter()                                │
│                                                                   │
│  ┌─────────────────────────────────────────────────────┐        │
│  │ Auth Component: Is user logged in?                  │        │
│  └──────────────────┬──────────────────────────────────┘        │
│                     │                                            │
│              YES ───┤─── NO → Redirect to /users/login          │
│                     │                                            │
└─────────────────────┼────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                    isAuthorized()                                │
│                  Main Authorization Gate                         │
│                                                                   │
│  ┌────────────────────────────────────────────────────┐         │
│  │ Is user administrator?                             │         │
│  └──────────────────┬─────────────────────────────────┘         │
│                     │                                            │
│              YES → ALLOW (bypass all checks)                     │
│                     │                                            │
│              NO ────┤                                            │
│                     ▼                                            │
│  ┌────────────────────────────────────────────────────┐         │
│  │ hasPermission($controller, $action)                │         │
│  │                                                     │         │
│  │  1. Get role permissions                           │         │
│  │  2. Check controller in permission map             │         │
│  │  3. Check action allowed (or '*' for all)          │         │
│  └──────────────────┬─────────────────────────────────┘         │
│                     │                                            │
│         DENIED ────┤──── ALLOWED                                │
│                     │         │                                  │
│                     ▼         ▼                                  │
│   ┌─────────────────────┐ ┌──────────────────────────┐         │
│   │handleUnauthorized   │ │ Continue to role-specific │         │
│   │Access()             │ │ authorization methods     │         │
│   │                     │ └───────────┬───────────────┘         │
│   │ - Log attempt       │             │                         │
│   │ - Set Flash message │             ▼                         │
│   │ - Redirect          │    ┌────────────────────┐            │
│   └─────────────────────┘    │ authorizeManagement│            │
│            │                  │ authorizeTmm...    │            │
│            │                  │ authorizeLpk...    │            │
│            ▼                  └─────────┬──────────┘            │
│       DENY ACCESS                       │                       │
│                                         ▼                       │
│                                   ALLOW ACCESS                  │
└─────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    beforeRender()                                │
│                  Menu Filtering                                  │
│                                                                   │
│  ┌────────────────────────────────────────────────────┐         │
│  │ Get allowed controllers from permissions           │         │
│  └──────────────────┬─────────────────────────────────┘         │
│                     ▼                                            │
│  ┌────────────────────────────────────────────────────┐         │
│  │ Filter menus WHERE controller IN (allowed)         │         │
│  │             OR controller IS NULL (parent menus)   │         │
│  └──────────────────┬─────────────────────────────────┘         │
│                     ▼                                            │
│  ┌────────────────────────────────────────────────────┐         │
│  │ Recursively filter child menus                     │         │
│  └──────────────────┬─────────────────────────────────┘         │
│                     ▼                                            │
│             Set $navigationMenus                                 │
└─────────────────────┼────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Render View                                     │
│              (Only authorized menus visible)                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Permission Check Flow

```
hasPermission($controller, $action)
          │
          ▼
┌──────────────────────────┐
│ Get current user role(s) │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ getRolePermissions()                 │
│                                      │
│  management?     → getManagementPerm │
│  tmm-recruitment → getRecruitmentPerm│
│  tmm-training   → getTrainingPerm    │
│  tmm-documentation → getDocPerm      │
│  lpk-penyangga  → getLpkPerm         │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ Permission Map Example:              │
│                                      │
│ [                                    │
│   'Candidates' => ['*'],            │
│   'CandidateDocuments' => ['*'],    │
│   'Trainees' => ['index', 'view']   │
│ ]                                    │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ Check if $controller exists in map  │
└────────┬─────────────────────────────┘
         │
    NO ──┤─── YES
         │      │
         ▼      ▼
    DENY  ┌─────────────────────────┐
          │ Check if action allowed │
          │                         │
          │ '*' → ALL ALLOWED       │
          │ OR action in array?     │
          └────────┬────────────────┘
                   │
              NO ──┤─── YES
                   │      │
                   ▼      ▼
               DENY    ALLOW
```

---

## 🏢 Institution Scope Validation

```
canAccessRecord($entity, $field)
          │
          ▼
┌──────────────────────────┐
│ Is user administrator?   │
└────────┬─────────────────┘
         │
    YES ─┤─ NO
         │   │
         ▼   ▼
     ALLOW  ┌────────────────────────────┐
            │ Is LPK user?               │
            │ (lpk-penyangga/lpk-so)     │
            └────────┬───────────────────┘
                     │
                NO ──┤── YES
                     │    │
                     ▼    ▼
                 ALLOW  ┌──────────────────────────┐
                        │ Get user institution_id  │
                        │ Get record institution   │
                        │ (from $entity->$field)   │
                        └────────┬─────────────────┘
                                 │
                                 ▼
                        ┌──────────────────────┐
                        │ institution_id ==    │
                        │ record institution?  │
                        └────────┬─────────────┘
                                 │
                            NO ──┤── YES
                                 │    │
                                 ▼    ▼
                             DENY   ALLOW

Example:
  User: lpk_bekasi (institution_id = 5)
  Record: candidate.vocational_training_institution_id = 5
  Result: ALLOW

  User: lpk_bekasi (institution_id = 5)
  Record: candidate.vocational_training_institution_id = 1
  Result: DENY (different institution)
```

---

## 📋 Menu Filtering Flow

```
beforeRender()
      │
      ▼
┌──────────────────────────┐
│ Is administrator?        │
└────────┬─────────────────┘
         │
    YES ─┤─ NO
         │   │
         ▼   ▼
    Load   ┌────────────────────────────┐
    ALL    │ getAllowedControllers()    │
    menus  │                            │
           │ Extract controller names   │
           │ from role permissions      │
           └────────┬───────────────────┘
                    │
                    ▼
           ┌────────────────────────────┐
           │ Example Result:            │
           │ [                          │
           │   'Candidates',            │
           │   'CandidateDocuments',    │
           │   'CandidateEducations',   │
           │   'Dashboard'              │
           │ ]                          │
           └────────┬───────────────────┘
                    │
                    ▼
           ┌────────────────────────────────────┐
           │ Query Menus WHERE                  │
           │   controller IN (allowed)          │
           │   OR controller IS NULL            │
           └────────┬───────────────────────────┘
                    │
                    ▼
           ┌────────────────────────────────────┐
           │ Contain ChildMenus (recursive)     │
           │ Same filter applied to children    │
           └────────┬───────────────────────────┘
                    │
                    ▼
           ┌────────────────────────────────────┐
           │ Result: Filtered menu tree         │
           │                                    │
           │ Dashboard                          │
           │ ├─ Candidates                     │
           │ │  ├─ List Candidates             │
           │ │  └─ Add Candidate               │
           │ └─ Candidate Documents            │
           │    ├─ List Documents              │
           │    └─ Add Document                │
           │                                    │
           │ (Trainees menu NOT included)       │
           └────────────────────────────────────┘
```

---

## 🎭 Role-Based Access Matrix

```
┌─────────────────┬──────────┬──────────┬────────────┬────────────┬─────────┐
│ Controller      │  Admin   │ Mgmt     │ Recruit    │ Training   │  LPK    │
├─────────────────┼──────────┼──────────┼────────────┼────────────┼─────────┤
│ Candidates      │    ✅    │   👁️   │     ✅     │    👁️    │   🏢    │
│                 │   All    │  View    │    All     │   View     │ Scoped  │
├─────────────────┼──────────┼──────────┼────────────┼────────────┼─────────┤
│ CandidateDocs   │    ✅    │   👁️   │     ✅     │     ❌     │   🏢    │
│                 │   All    │  View    │    All     │    No      │ Scoped  │
├─────────────────┼──────────┼──────────┼────────────┼────────────┼─────────┤
│ Trainees        │    ✅    │   👁️   │     ❌     │     ✅     │    ❌   │
│                 │   All    │  View    │    No      │    All     │   No    │
├─────────────────┼──────────┼──────────┼────────────┼────────────┼─────────┤
│ TraineeDocs     │    ✅    │   👁️   │     ❌     │     ✅     │    ❌   │
│                 │   All    │  View    │    No      │    All     │   No    │
├─────────────────┼──────────┼──────────┼────────────┼────────────┼─────────┤
│ Apprentices     │    ✅    │   👁️   │     ✅     │     ❌     │    ❌   │
│                 │   All    │  View    │    All     │    No      │   No    │
├─────────────────┼──────────┼──────────┼────────────┼────────────┼─────────┤
│ ApprenticeOrders│    ✅    │   👁️   │     ✅     │     ❌     │    ❌   │
│                 │   All    │  View    │    All     │    No      │   No    │
├─────────────────┼──────────┼──────────┼────────────┼────────────┼─────────┤
│ Master Tables   │    ✅    │   👁️   │    👁️    │    👁️    │   👁️  │
│                 │   All    │  View    │   View     │   View     │  View   │
└─────────────────┴──────────┴──────────┴────────────┴────────────┴─────────┘

Legend:
  ✅ = Full Access (index, view, add, edit, delete, export)
  👁️ = Read-Only (index, view, export only)
  🏢 = Institution-Scoped (full access but only own institution's data)
  ❌ = No Access (completely blocked)
```

---

## 🔄 Data Flow in Index Action

```
User Request: GET /candidates/index
          │
          ▼
┌───────────────────────────────────┐
│ CandidatesController::index()    │
└────────┬──────────────────────────┘
         │
         ▼
┌───────────────────────────────────┐
│ isAuthorized() check             │
│ - Administrator? → PASS           │
│ - Has permission? → PASS          │
│ - Role-specific auth → PASS       │
└────────┬──────────────────────────┘
         │
         ▼
┌───────────────────────────────────┐
│ Build Query                       │
│                                   │
│ $query = $this->Candidates        │
│   ->find()                        │
└────────┬──────────────────────────┘
         │
         ▼
┌───────────────────────────────────┐
│ Apply Institution Filter          │
│ (for LPK users only)              │
│                                   │
│ if (hasRole('lpk-penyangga')) {   │
│   $institutionId = getUserInst(); │
│   $query->where([                 │
│     'vocational_training_'        │
│     'institution_id' => $id       │
│   ]);                             │
│ }                                 │
└────────┬──────────────────────────┘
         │
         ▼
┌───────────────────────────────────┐
│ Execute Query                     │
│                                   │
│ Results:                          │
│ - Admin: ALL candidates           │
│ - LPK:   Only own institution     │
└────────┬──────────────────────────┘
         │
         ▼
┌───────────────────────────────────┐
│ beforeRender()                    │
│ - Filter menus by role            │
│ - Set $navigationMenus            │
└────────┬──────────────────────────┘
         │
         ▼
┌───────────────────────────────────┐
│ Render View                       │
│ - Display candidates table        │
│ - Show only authorized menus      │
│ - Hide unauthorized action buttons│
└───────────────────────────────────┘
```

---

## ⚠️ Unauthorized Access Handling

```
User tries: GET /trainees/index
(User role: lpk-penyangga)
          │
          ▼
┌────────────────────────────────────────┐
│ isAuthorized()                         │
│                                        │
│ 1. Administrator? → NO                 │
│ 2. hasPermission('Trainees', 'index')  │
│    → Check permission map              │
│    → 'Trainees' NOT in LPK permissions │
│    → PERMISSION DENIED                 │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│ handleUnauthorizedAccess()             │
│                                        │
│ 1. Get user info:                      │
│    - username: lpk_bekasi              │
│    - role: lpk-penyangga               │
│    - action: index                     │
│    - controller: Trainees              │
│                                        │
│ 2. Log warning:                        │
│    "Unauthorized access attempt:       │
│     User=lpk_bekasi,                   │
│     Role=lpk-penyangga,                │
│     Action=index,                      │
│     Controller=Trainees,               │
│     Reason=No access to Trainees"      │
│                                        │
│ 3. Set Flash message:                  │
│    ┌──────────────────────────────┐   │
│    │ ❌ Access Denied             │   │
│    │                              │   │
│    │ Your role does not have      │   │
│    │ access to index in Trainees  │   │
│    │                              │   │
│    │ Your account: lpk_bekasi     │   │
│    │ Your role: lpk-penyangga     │   │
│    │                              │   │
│    │ Please contact administrator │   │
│    │ or [login with different     │   │
│    │      account]                │   │
│    └──────────────────────────────┘   │
│                                        │
│ 4. Redirect to referer or dashboard    │
└────────┬───────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────┐
│ User sees error message                │
│ Redirected to Dashboard                │
└────────────────────────────────────────┘
```

---

## 📊 Security Logging Flow

```
┌─────────────────────────────────────────┐
│ Every Unauthorized Attempt              │
│                                         │
│ handleUnauthorizedAccess() called       │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ Collect Context Data                    │
│                                         │
│ - timestamp                             │
│ - username                              │
│ - role(s)                               │
│ - controller                            │
│ - action                                │
│ - reason for denial                     │
│ - IP address (from request)             │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ Write to logs/error.log                 │
│                                         │
│ Format:                                 │
│ [YYYY-MM-DD HH:MM:SS] Warning:          │
│ Unauthorized access attempt:            │
│ User=username, Role=role,               │
│ Action=action, Controller=controller,   │
│ Reason=reason                           │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│ Administrator Can:                      │
│                                         │
│ - Monitor security logs                 │
│ - Identify repeated attempts            │
│ - Track which users need more access    │
│ - Audit permission effectiveness        │
└─────────────────────────────────────────┘
```

---

## 🎯 Complete User Journey Example

**Scenario**: LPK user tries to add trainee (unauthorized)

```
1. User navigates application
   └─> Menu: No "Trainees" option visible (filtered in beforeRender)

2. User types URL directly: /trainees/add
   └─> Request received

3. beforeFilter() - Authentication
   └─> User logged in? YES → Continue

4. isAuthorized() - Authorization
   ├─> Administrator? NO
   ├─> hasPermission('Trainees', 'add')?
   │   ├─> Get LPK permissions
   │   │   └─> ['Candidates', 'CandidateDocuments', 'Dashboard']
   │   └─> 'Trainees' NOT in list → DENIED
   │
   └─> handleUnauthorizedAccess('add', 'No access to Trainees')

5. Security Logging
   └─> Log entry created in logs/error.log

6. User Notification
   ├─> Flash message set with details
   └─> Redirect to dashboard

7. User Experience
   ├─> Sees detailed error message
   ├─> Understands why access denied
   ├─> Knows current role (lpk-penyangga)
   └─> Has link to login with different account

8. Administrator Review (later)
   ├─> Checks logs/error.log
   ├─> Sees repeated attempts by lpk_bekasi
   └─> Can decide: Grant permission or educate user
```

---

## 📈 Permission Hierarchy

```
┌──────────────────────────────────────────────────────────────┐
│                     Administrator                             │
│                (Full Access - Bypasses All)                   │
└──────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│   Management     │  │  TMM Department  │  │   LPK Users      │
│  (Read-Only)     │  │   (Department    │  │ (Institution-    │
│                  │  │    Specific)     │  │   Scoped)        │
│ • View all data  │  │                  │  │                  │
│ • Export reports │  │ ┌──────────────┐ │  │ • Full CRUD on   │
│ • No editing     │  │ │ Recruitment  │ │  │   Candidates     │
│                  │  │ │ • Candidates │ │  │ • Own institution│
│                  │  │ │ • Apprentices│ │  │   only           │
│                  │  │ └──────────────┘ │  │                  │
│                  │  │                  │  │                  │
│                  │  │ ┌──────────────┐ │  │                  │
│                  │  │ │  Training    │ │  │                  │
│                  │  │ │ • Trainees   │ │  │                  │
│                  │  │ └──────────────┘ │  │                  │
│                  │  │                  │  │                  │
│                  │  │ ┌──────────────┐ │  │                  │
│                  │  │ │Documentation │ │  │                  │
│                  │  │ │ • All Docs   │ │  │                  │
│                  │  │ └──────────────┘ │  │                  │
└──────────────────┘  └──────────────────┘  └──────────────────┘

Permission Levels:
  ⬛ Level 0: No access (blocked)
  🟨 Level 1: Read-only (view, export)
  🟩 Level 2: Department-specific (full access to assigned area)
  🟦 Level 3: Institution-scoped (full access within own institution)
  🟪 Level 4: Full system access (administrator)
```

This visual guide helps understand the complete authorization system flow and hierarchy.
